langcode: en
status: true
dependencies:
  config:
    - group.type.country
  module:
    - user
id: country-group_membership
label: 'Country: Group membership'
description: 'Adds users to groups as members.'
group_type: country
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
