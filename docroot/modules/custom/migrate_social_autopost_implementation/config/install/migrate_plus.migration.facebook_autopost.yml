id: facebook_autopost
label: Migrate nodes to facebook
migration_group: social_networks_autopost
source:
  plugin: d8_node_social
  social_type: facebook
  track_changes: true
destination:
  plugin: social_destination
  social_network_plugin: facebook

process:
  # Note that the source field names here (machine_name and friendly_name) were
  # defined by the 'fields' configuration for the source plugin above.
  id: id
  message: title
