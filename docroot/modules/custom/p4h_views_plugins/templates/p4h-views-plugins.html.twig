{% if items.previous or items.next %}
  <nav role="navigation" aria-labelledby="pagination-heading">
    <h4 class="visually-hidden">{{ 'Pagination'|t }}</h4>
    <ul class="js-pager__items">
      <li class="font-social-icon arrow-left-icon prev">
        {% if items.previous %}
          <a href="{{ items.previous.href }}" title="{{ 'Go to previous page'|t }}" rel="prev"{{ items.previous.attributes|without('href', 'title', 'rel') }}>
            <span class="visually-hidden">{{ 'Previous page'|t }}</span>
            <span aria-hidden="true">{{ items.previous.text|default('‹‹'|t) }}</span>
          </a>
        {% endif %}
      </li>
      {% if items.min or items.max  %}
        <li>
          {% trans %}
            {{ items.min }} - {{ items.max }} out of {{ options.total_rows }}
          {% endtrans %}
        </li>
      {% endif %}
      <li class="font-social-icon arrow-right-icon next">
        {% if items.next %}
          <a href="{{ items.next.href }}" title="{{ 'Go to next page'|t }}" rel="next"{{ items.next.attributes|without('href', 'title', 'rel') }}>
            <span class="visually-hidden">{{ 'Next page'|t }}</span>
            <span aria-hidden="true">{{ items.next.text|default('››'|t) }}</span>
          </a>
        {% endif %}
      </li>
    </ul>
  </nav>
{% endif %}
