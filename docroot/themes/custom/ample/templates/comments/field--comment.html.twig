<section{{ attributes }}>
  {% if comment_status %}
    <h2{{ title_attributes }}>{{ 'Comments'|t }} ({{ comment_count }})</h2>
  {% endif %}

  {% if comment_form %}
    <div class="title-wrapper">
      <h2{{ content_attributes }}>{{ 'Write comment'|t }}</h2>
      <div class="line"></div>
    </div>
    {{ comment_form }}
  {% endif %}

  {% for item in items %}
    {{ item.content['comments'] }}
  {% endfor %}
</section>
