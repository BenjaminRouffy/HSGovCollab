{% for item in items %}
  {% set region = item.content|render %}
  {% if region is not empty %}
    <span {{ item.attributes.addClass('project-bottom-link font-social-icon country-icon') }}>
      <span>{{ "Part of the @region Region"|t({ '@region': region }) }}</span>
    </span>
  {% endif %}
{% endfor %}
