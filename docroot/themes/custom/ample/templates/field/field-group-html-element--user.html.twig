{#
/**
 * @file
 * Default theme implementation for a fieldgroup html element.
 *
 * Available variables:
 * - title: Title of the group.
 * - title_element: Element to wrap the title.
 * - children: The children of the group.
 * - wrapper_element: The html element to use
 * - attributes: A list of HTML attributes for the group wrapper.
 *
 * @see template_preprocess_field_group_html_element()
 *
 * @ingroup themeable
 */
#}

<{{ wrapper_element }} {{ attributes.setAttribute('data-anchor-id', title).setAttribute('data-region', '.region-wrapper .content .anchor-region:eq(0)') }}>
  {% if title %}
  <{{ title_element }} class="anchor-title{% if collapsible %} field-group-toggler{% endif %}">{{ title }}</{{ title_element }}>
  {% endif %}
  <div class="anchor-region"></div>
  {% if collapsible %}
  <div class="field-group-wrapper">
  {% endif %}
  {{children}}
  {% if collapsible %}
  </div>
  {% endif %}
</{{ wrapper_element }}>
