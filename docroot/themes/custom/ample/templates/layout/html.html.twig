{% spaceless %}
  {% set classes = [] %}

  {% if logged_in %}
    {% set classes = ['logged']|merge(classes) %}
  {% endif %}

  {% if is_front %}
    {% set classes = ['front']|merge(classes) %}
  {% endif %}

  {% if root_path %}
    {% set classes = [root_path|clean_class]|merge(classes) %}
  {% endif %}
{% endspaceless %}
<!DOCTYPE html>
<html{{ html_attributes }}>
<head>
  <head-placeholder token="{{ placeholder_token|raw }}">
    <title>
      {{ head_title|safe_join(' | ') }}
    </title>
    <css-placeholder token="{{ placeholder_token|raw }}">
      <js-placeholder token="{{ placeholder_token|raw }}">
        <link href="https://fonts.googleapis.com/css?family=Fira+Sans:300,400,700|Libre+Franklin:400,600|Oswald:300,400,700" rel="stylesheet">
        </head>
<body{{ attributes.addClass(classes) }} role="document">
{{ page_top }}
{{ page }}
{{ page_bottom }}
<js-bottom-placeholder token="{{ placeholder_token|raw }}">
</body>
</html>
